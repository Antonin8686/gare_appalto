# Copia in scripts/deploy.config.ps1 (gitignored) oppure passa i parametri a deploy-to-vps.ps1

$VpsHost = "82.165.176.194"
$VpsUser = "root"
$VpsPath = "/var/www/gare-appalto"
$ComposeProjectName = "gareappalto"
$SiteUrl = "http://gare.fontanebianche.today:8080"

# Opzionale: password SSH (solo locale, mai committare)
# $VpsSshPassword = "..."
